---
title: AI掘金头条-收藏和浏览历史
domain: IT_Technology
tags: [FastAPI, 项目实战, 收藏, 浏览历史, 联表查询]
status: 稳定
created: 2026-06-19
updated: 2026-06-19
source: "[[尚硅谷-FastAPI-基础到实战]]"
related: [[04-AI掘金头条-新闻模块]], [[05-AI掘金头条-用户模块]]
summary: "AI掘金头条收藏与浏览历史模块实现检查收藏状态、添加/取消收藏、收藏列表（联表查询+分页）、清空收藏，以及浏览历史的增删查清空，核心涉及 ORM join 联表查询与 UniqueConstraint 唯一约束。"
---

# AI掘金头条 — 收藏与浏览历史

> 检查新闻收藏状态、添加和取消收藏、收藏列表、清空收藏 / 添加浏览记录、浏览历史列表、删除单条、清空浏览历史

---

## 一、收藏模块

### 路由前缀

```python
router = APIRouter(prefix="/api/favorite", tags=["favorite"])
```

---

### 1.1 检查新闻收藏状态

```python
@router.get("/check")
async def check_favorite(
    news_id: int = Query(..., alias="newsId"),
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    return success_response(data=...)
```

**业务流程：**

```
进入请求 → 验证 Token（已登录）
          → 按 user_id + news_id 联查收藏表
          → 返回是否已收藏
```

```python
select(Favorite).where(
    Favorite.user_id == user_id,
    Favorite.news_id == news_id
)
```

---

### 1.2 添加收藏

```python
@router.post("/add")
async def add_favorite(
    data: FavoriteAddRequest,
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    return success_response(message="收藏成功", data=...)
```

**业务流程：**

```
进入请求 → 验证 Token → add() 添加收藏记录 → 响应
```

**参数：** 请求体参数 `newsId`

---

### 1.3 取消收藏

```python
@router.delete("/remove")
async def remove_favorite(
    news_id: int = Query(..., alias="newsId"),
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    return success_response(message="取消收藏成功")
```

**业务流程：**

```
进入请求 → 验证 Token → delete() 删除收藏记录
          → 检查命中数量 > 0 → 响应（否则抛出异常）
```

**参数：** 查询参数 `newsId`

---

### 1.4 获取收藏列表

```python
@router.get("/list")
async def get_favorite_list(
    page: int = Query(1, ge=1),
    page_size: int = Query(10, ge=1, le=100, alias="pageSize"),
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    return success_response(data=...)
```

**业务流程：**

```
进入请求 → 验证 Token → 联表查询收藏新闻 → 统计收藏总量 → 计算是否有更多 → 响应
```

**联表查询：**

```python
select(News).join(Favorite)
# 统计收藏总量
func.count()
```

---

### 1.5 清空收藏列表

```python
@router.delete("/clear")
async def clear_favorite(
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    return success_response(message=f"清空{xx}条收藏记录")
```

**业务流程：**

```
进入请求 → 验证 Token → delete() 清空当前用户收藏 → result.rowcount 获取删除数量 → 响应
```

---

## 二、收藏 vs 浏览历史

| 收藏 | 浏览历史 |
|------|----------|
| 添加收藏 | 添加浏览记录 |
| 取消收藏 | 删除单条浏览记录 |
| 获取收藏列表 | 获取浏览历史列表 |
| 清空收藏列表 | 清空浏览历史列表 |

---

## 三、浏览历史模块

### 路由前缀

```python
router = APIRouter(prefix="/api/history", tags=["history"])
```

---

### 3.1 添加浏览记录

```python
@router.post("/add")
async def add_history(
    data: HistoryAddRequest,
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    return success_response(message="添加成功", data=...)
```

**业务流程：**

```
进入请求 → 验证 Token → 检查是否已浏览过当前新闻
          → 是 → 更新浏览时间
          → 否 → add() 添加历史记录 → 响应
```

**参数：** 请求体参数 `newsId`

---

### 3.2 获取浏览历史列表

```python
@router.get("/list")
async def get_history_list(
    page: int = Query(1, ge=1),
    page_size: int = Query(10, ge=1, le=100, alias="pageSize"),
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    return success_response(data=...)
```

**业务流程：**

```
进入请求 → 验证 Token → 联表查询浏览历史 → 统计总量 → 计算是否有更多 → 响应
```

```python
select(News).join(History)
# 统计总量
func.count()
```

---

### 3.3 删除单条浏览记录

```python
@router.delete("/delete/{history_id}")
async def delete_history(
    history_id: int,
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    return success_response(message="删除成功")
```

**业务流程：**

```
进入请求 → 验证 Token → delete() 删除指定浏览记录 → 检查命中数量 > 0 → 响应
```

---

### 3.4 清空浏览历史列表

```python
@router.delete("/clear")
async def clear_history(
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    return success_response(message="清空成功")
```

**业务流程：**

```
进入请求 → 验证 Token → delete() 清空当前用户浏览记录 → 响应
```

---

## 四、核心知识点

### 4.1 ORM 联表查询

```python
select(
    News,
    Favorite.created_at.label("favorite_time"),
    Favorite.id.label("favorite_id")
).join(Favorite, Favorite.news_id == News.id)
```

- `select(主模型类, 字段.label("别名"))` — 查询主体并可对关联字段重命名
- `.join(关联模型类, join 条件)` — 联表连接

### 4.2 UniqueConstraint 唯一约束

```python
class Favorite(Base):
    __table_args__ = (
        UniqueConstraint('user_id', 'news_id', name='user_news_unique'),
    )
```

确保同一个用户不能重复收藏同一条新闻。

---

## 关联笔记

- [[04-AI掘金头条-新闻模块]] — 新闻分类、列表、详情
- [[05-AI掘金头条-用户模块]] — 用户注册、登录、Token 认证
- [[02-FastAPI进阶]] — ORM CRUD 操作详解
- [[Python-MOC]] — Python 知识总导航
