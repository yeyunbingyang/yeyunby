---
title: Agent Skills 快速说明
domain: Core_Ability
tags: [AI, Agent, Skills, 基础]
status: 稳定
created: 2026-07-22
updated: 2026-07-23
related:
  - "03-通用skills最佳实践"
  - "02-我的skills蒸馏"
summary: "Agent Skills 的核心概念——什么是 Skills、为什么需要它、底层设计思路。详细最佳实践和调用机制见 03-通用skills最佳实践。"
---

# Agent Skills 快速说明

## 一句话总结

Agent Skills 是一种**带目录的说明书**——让 AI 代理知道自己有哪些能力，在什么时机调用，但只在需要时才加载详细指令。

核心设计是**渐进式披露（Progressive Disclosure）**：元数据常驻、指令按需加载、资源按需调用。

```
元数据（name + description）→ 常驻上下文，Token 消耗极低
指令层（SKILL.md 正文）     → AI 判断匹配后才加载
资源层（scripts/references） → 执行时才调用，不进入上下文
```

> 详细的三层结构说明、调用机制、MCP 对比、最佳实践见 [[03-通用skills最佳实践]]。

## 文件结构

```
项目根目录/
└── .claude/
    └── skills/
        └── 技能名称/
            ├── skill.md      ← 定义文件（必须）
            ├── scripts/      ← 可执行脚本（可选）
            ├── references/   ← 参考文档（可选）
            └── assets/       ← 图片等资源（可选）
```

**注意**：文件名必须是 `skill.md`（S 大写），description 中必须写清楚调用时机。

## 核心优势

- **Token 高效**：只加载目录，需要时才加载正文
- **降低提示词复杂度**：避免一次性塞入过多内容
- **渐进式决策**：AI 根据判断决定是否需要深入调用

## 跨平台配置

| 工具 | 配置路径 | 备注 |
|------|---------|------|
| Claude Code | `.claude/skills/` 或 `~/.claude/skills/` | 原生支持 |
| Codex | `~/.codex/skills/` | 需开启实验性功能 |
| Hermes Agent | `~/AppData/Local/hermes/skills/` | 通过 skill_manage 注册 |

## 社区资源

- **Awesome Claude Skills**（GitHub 1.4 万+ Star）：大量现成 skill
- 使用方式：下载后直接将 skill 文件夹拖入本地 skills 目录

## 关联笔记

- [[03-通用skills最佳实践]] — Skills 使用心法与实战案例（推荐先读这篇）
- [[02-我的skills蒸馏]] — 个人 Skills 的持续记录与优化
- [[04-Skills生态全景]] — Skills 生态的全景图
