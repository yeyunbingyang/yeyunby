# GPT 图片提示词适配

GPT 更适合接收自然、明确、带编辑意图的指令。优先描述目标结果和需要保持的约束，不依赖权重符号或参数串。

## 文生图顺序

用途与画幅 → 主体 → 动作与表情 → 场景 → 构图与镜头 → 光线与色彩 → 材质与风格 → 必须避免的内容。

## 参考图编辑顺序

1. 明确以哪张图为基础。
2. 列出必须保持不变的元素。
3. 列出需要修改的元素。
4. 描述修改后的整体画面。
5. 说明不得新增、删除或改变的内容。

使用完整句子表达空间关系和修改边界，避免只堆关键词。
